# Coherent Business Analytics Plugin Placeholder

This folder is reserved for a future Coherent Business Analytics persona plugin.

When the course and workflow package are ready, replace this placeholder with a complete Codex plugin folder containing:

- `.codex-plugin/plugin.json`
- `skills/`
- `docs/`
- `assets/`

After the plugin is ready, add it to `.agents/plugins/marketplace.json` so Codex can install it from the Coherent Marketplace.
